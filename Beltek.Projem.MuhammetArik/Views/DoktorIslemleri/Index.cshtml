@model IEnumerable<Beltek.Projem.MuhammetArik.Models.Doktor>

@{
    ViewData["Title"] = "Doktor İşlemleri";
}

<h1>Doktor İşlemleri</h1>
<p>
    <a class="bi bi-arrow-right-circle buttonStyle" asp-action="Create">Doktor Ekle</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Soyad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody class="innerBorder">
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Ad)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Soyad)
                </td>
                <td>
                    <a class="bi bi-pencil-square d-inline p-2 bg-success text-white" asp-action="Edit" asp-route-id="@item.DoktorId">Düzenle</a> |
                    <a class="bi bi-info-circle d-inline p-2 bg-info text-white" asp-action="Details" asp-route-id="@item.DoktorId">Detaylar</a> |
                    <a class="bi bi-trash d-inline p-2 bg-danger text-white" asp-action="Delete" asp-route-id="@item.DoktorId">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>